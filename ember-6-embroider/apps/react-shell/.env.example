# Development environment variables
# Copy this file to .env for local development: cp .env.example .env

# API
API_URL=http://localhost:3333/api

# Ember App (Module Federation ESM Remote)
# IMPORTANT: Must match the port where Ember dev server runs
# NOTE: No trailing slash - paths will be added in code
# Used for: remoteEntry.js, CSS, and assets
EMBER_APP_URL=http://localhost:4200

# Module Federation Remotes
# IMPORTANT: Must match the ports where MFE dev servers run
PROFILE_REMOTE_URL=http://localhost:3001/remoteEntry.js
BLOG_ARTICLE_REMOTE_URL=http://localhost:3002/remoteEntry.js
